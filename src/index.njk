---
permalink: "index.html"
---

{% extends "src/base.njk" %}

{% block head %}
<title>Partidos Públicos | Red Ciudadana</title>

{% endblock %}

{% block content %}
	<!--Contenido-->
	<div class="section m-0 bg-home">
		<div class="container">
			<div class="row">
				<!--Texto-->
				<div></div>
			</div>
		</div>
	</div>
	<div class="section">
		<div class="container">
			<div class="row">
				<div class="col-lg-6" style="display:grid;align-content:center">
					<h3 style="font-weight:600;">¿Qué es Partidos Públicos</h3>
					<p>Partidos Públicos es una plataforma que busca facilitar el acceso 
						a la fiscalización ciudadana a la información pública que publican los
						partidos políticos guatemaltecos en transparencia activa, a través 
						de visualizaciones, filtros y comparativas amigables. !Queremos 
						que la trnasparencia sea útil y entendible!</p>
				</div>
				<div class="col-lg-6 d-flex justify-content-center">
					{% for video in collections.videos%}
					<div class="video">
						<iframe style="width:500px!important" height="330px" src="{{video.data.link}}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
					</div>
					{% endfor %}
				</div>
			</div>
		</div>
	</div>

	<div class="section m-0" style="background-color:#f1f3fd">
		<div class="container">
			<div class="row my-6">
				<div class="col-lg-8 my-4">
					<h3 style="font-weight:600">Blogs</h3>
					{% for novedad in collections.novedades%}
					<div class="novedad">
						<img src="{{novedad.data.image}}">
						<div class="texto">
							<h6 class="text-left">{{novedad.data.fecha | dateFormat('DD/MM/YYYY')}}</h6>
							<h3>{{novedad.data.title}}</h3>
							<p>{{novedad.data.autor}}</p>
						</div>
					</div>
					<div class="novedad">
						<img src="{{novedad.data.image}}">
						<div class="texto">
							<h6 class="text-left">{{novedad.data.fecha | dateFormat('DD/MM/YYYY')}}</h6>
							<h3>{{novedad.data.title}}</h3>
							<p>{{novedad.data.autor}}</p>
						</div>
					</div>
					<div class="novedad">
						<img src="{{novedad.data.image}}">
						<div class="texto">
							<h6 class="text-left">{{novedad.data.fecha | dateFormat('DD/MM/YYYY')}}</h6>
							<h3>{{novedad.data.title}}</h3>
							<p>{{novedad.data.autor}}</p>
						</div>
					</div>
					<div class="novedad">
						<img src="{{novedad.data.image}}">
						<div class="texto">
							<h6 class="text-left">{{novedad.data.fecha | dateFormat('DD/MM/YYYY')}}</h6>
							<h3>{{novedad.data.title}}</h3>
							<p>{{novedad.data.autor}}</p>
						</div>
					</div>
					{% endfor %}

				</div>
				<div class="col-lg-4 my-4">
					<h3 style="font-weight:600">Videos</h3>
					{% for video in collections.videos%}
					<div class="video">
						<iframe height="200px" src="{{video.data.link}}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
					</div>
					<div class="video">
						<iframe height="200px" src="{{video.data.link}}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
					</div>
					<div class="video">
						<iframe height="200px" src="{{video.data.link}}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
					</div>
					{% endfor %}
				</div>
			</div>
		</div>
	</div>
{% endblock %}

{% block body_javascripts %}
	
{% endblock %}